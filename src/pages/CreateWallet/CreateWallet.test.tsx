describe("CreateWallet", () => {
  test("renders input for name and button", () => {});
  test("wallet name is required", () => {});
  test("creates a wallet and navigates to the wallets list", () => {});
});
